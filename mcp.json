{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "campus-unr": {
      "type": "stdio",
      "command": "./bin/campus-server",
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
